# renderLoading

Render loading status to Command App.

```ts
declare function renderLoading(data?: LoadingType): void
```

## Usage

```ts
import { renderLoading } from 'penx'

export async function main() {
  renderLoading(content)
}
```
